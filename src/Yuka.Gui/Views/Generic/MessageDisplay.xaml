<UserControl x:Class="Yuka.Gui.Views.Generic.MessageDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="300" Name="This">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Image Grid.Row="0" Source="{Binding Icon, ElementName=This}" Width="48" Height="48"/>
        <TextBlock Grid.Row="1" FontSize="14" Text="{Binding Message, ElementName=This}" TextAlignment="Center"/>
    </Grid>
</UserControl>
